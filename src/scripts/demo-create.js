const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient();
async function createDemoProducts() { try { const elektronikGrup = await prisma.urunHizmetGruplari.upsert({ where: { grupKodu: 'ELKT' }, update: {}, create: { grupTipi: 'URUN', grupKodu: 'ELKT', grupAdi: 'Elektronik', aciklama: 'Elektronik ürünler', aktif: true, sira: 1 } }); console.log('Elektronik grup oluşturuldu:', elektronikGrup); const urun = await prisma.urunler.create({ data: { urunKodu: 'ELKT-001', urunAdi: 'Dizüstü Bilgisayar', grupId: elektronikGrup.id, birim: 'Adet', birimFiyat: 5000, kdvOrani: 18, aciklama: 'Yüksek performanslı iş bilgisayarı', aktif: true } }); console.log('Ürün eklendi:', urun); console.log('İşlem tamamlandı!'); } catch (error) { console.error('Hata:', error); } finally { await prisma.(); } } createDemoProducts();
